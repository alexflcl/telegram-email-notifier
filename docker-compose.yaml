version: '3.8'

services:
  backend:
    image: alexflcl/telegram-email-notifier:latest
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///app/data/telegram_notifier.db
      - PYTHONUNBUFFERED=1